# Workspace Rules for LinkedAI

## Post-Update Workflow
- Always stage (`git add`), commit (`git commit -m "..."`), and push (`git push`) changes to the repository immediately after completing any code or data update.
